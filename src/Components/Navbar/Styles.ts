const selectedNavbarItemIcon = {
    color: 'white'
}

const selectedNavbarItemButton = {
    color: 'white !important',
    backgroundColor: '#f50 !important',
}

const navbarItemButton = {
    marginBottom: '1rem',
    padding: '.3rem',
    borderRadius: '.25rem',
}

const navbarList = {
    width: '90%',
    paddingTop: 0
}

export const styles = {
    selectedNavbarItemButton,
    selectedNavbarItemIcon,
    navbarItemButton,
    navbarList
}